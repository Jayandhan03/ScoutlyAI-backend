# ── Core Framework ──
fastapi
uvicorn[standard]
python-dotenv

# ── LLM & Agents ──
langchain-xai
langchain_tavily
langgraph

# ── TTS ──
edge-tts

# ── HTTP Client ──
requests

# ── Article text extraction (test.py) ──
trafilatura

# ── File Upload Support ──
python-multipart

# ── Database (shared MongoDB with the Next.js app) ──
pymongo[srv]

# ── Scheduler ──
apscheduler

# ── IANA timezone data (python:slim base images ship no system tzdata,
# so zoneinfo.ZoneInfo() on any real zone raises ZoneInfoNotFoundError
# and silently falls back to UTC wherever that's caught) ──
tzdata

# ── Push notifications ──
firebase-admin